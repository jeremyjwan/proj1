<h3>Trainer</h3>
<p>Name: <%= @trainer.name if @trainer.name %></p>
<p>Email: <%= @trainer.email if @trainer.email %></p>
<%= button_to "New!", new_path(id: @trainer.id), :class => "button medium", :method => :get if current_trainer == @trainer %>
<% @pokemons.each do |pokemon| %> 
	<p> <%= pokemon.name %> </p> 
	<% if pokemon.health > 0 %>
		<p> <%= pokemon.level %> </p>
		<p> <%= pokemon.health %> </p> 
	  	<%= button_to "Damage!", damage_path(id: pokemon.id), :class => "button medium", :method => :patch %>
	<% end %>
  	<%= button_to "Heal!", heal_path(id: pokemon.id), :class => "button medium", :method => :patch %>
<% end %> 